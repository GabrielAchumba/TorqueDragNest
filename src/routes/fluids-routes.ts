const controller = "Fluids";
const PostFluid = `PostFluid`;
const GetFluid_designId = `GetFluid/:designId`;
const PutFluid_designId = `PutFluid/:designId`;
const GetFluids = `GetFluids`;
const DeleteFluid_id = `DeleteFluid/:id`;

export { controller, PostFluid, GetFluid_designId,
    PutFluid_designId, GetFluids, DeleteFluid_id }