const controller = "identities";
const Login = `Login`;
const Logout = `Logout`;
const PostIdentity = `PostIdentity`;
const GetIdentities_id = `GetIdentities/:id`;
const PutIdentity_id = `PutIdentity/:id`;
const GetIdentities = `GetIdentities`;
const DeleteIdentity_id = `DeleteIdentity/:id`;

export { controller, Login, Logout, PostIdentity, GetIdentities_id,
    PutIdentity_id, GetIdentities, DeleteIdentity_id }