const controller = "Companies";
const PostCompany = `PostCompany`;
const GetCompanies_id = `GetCompanies/:id`;
const PutCompany_id = `PutCompany/:id`;
const GetCompanies = `GetCompanies`;
const DeleteCompany_id = `DeleteCompany/:id`;

export { controller, PostCompany, GetCompanies_id, 
    PutCompany_id, GetCompanies, DeleteCompany_id }