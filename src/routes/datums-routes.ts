const controller = "Datums";
const PostDatum = `PostDatum`;
const GetDatums_designId = `GetDatums/:designId`;
const PutDatum_id = `PutDatum/:id`;
const GetDatums = `GetDatums`;
const DeleteDatum_id = `DeleteDatum/:id`;

export { controller, PostDatum, GetDatums_designId,
    PutDatum_id, GetDatums, DeleteDatum_id }